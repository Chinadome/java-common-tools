什么是代理？什么是动态代理？
答: 代理是⼀种设计模式，它可以在⽬标对象的基础上，增加⼀些额外的功能，扩展⽬标对象的功能⽽不⽤修改已
   经存在的代码。
   动态代理：在程序运行时，通过反射机制动态生成。
   动态代理类通常代理接口下的所有类。
   动态代理事先不知道要代理的是什么，只有在运行的时候才能确定。
   动态代理的调用处理程序必须事先InvocationHandler接口
   ，及使用Proxy类中的newProxyInstance方法动态的创建代理类。
   Java动态代理只能代理接口，要代理类需要使用第三方的CLIGB等类库。
动态代理和cglib代理的区别？
    1.Java动态代理只能够对接口进行代理，不能对普通的类进行代理（因为所有生成的代理类的父类为Proxy，Java类继承机制不允许多重继承）；
    CGLIB能够代理普通类；
    2.Java动态代理使用Java原生的反射API进行操作，在生成类上比较高效；
    CGLIB使用ASM框架直接对字节码进行操作，在类的执行过程中比较高效
简述⼀下 Spring AOP 的实现？
答：被代理对象基于接口实现，使用JDK代理
    被代理对象未基于接口实现，使用Cglib代理。
